# Voice Writer Reference

The voice-writer skill lives at `skills/content/voice-writer/SKILL.md`.
The /do router discovers it through INDEX.json triggers and force_route
configuration.

See `skills/INDEX.json` entry for `voice-writer` for routing triggers and
skill metadata.
